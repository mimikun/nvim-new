---@type table
local opts = {
  opts = {
    -- Auto close tags
    enable_close = true,
    -- Auto rename pairs of tags
    enable_rename = true,
    -- Auto close on trailing </
    enable_close_on_slash = false,
  },
  per_filetype = {
    -- NOTE: it?
  },
}

return opts
