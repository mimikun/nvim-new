---@type GithubActionsConfig
local opts = {
  actions = {
    icons = {
      -- Icon for outdated versions (default)
      outdated = "",
      -- Icon for latest versions (default)
      latest = "",
      -- Icon for error (default)
      error = "",
    },
  },
}

return opts
