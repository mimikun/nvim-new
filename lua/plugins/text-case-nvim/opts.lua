---@type table
local opts = {
  prefix = "ga",
  -- If `substitude_command_name` is not nil, an additional command with the passed in name will be created that does the same thing as "Subs" does.
  substitude_command_name = nil,
}

return opts
